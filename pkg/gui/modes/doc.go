// Package modes is intentionally empty. The canonical modal-state store
// lives at pkg/gui/keys.ModeStore (see pkg/gui/keys/modestore.go); the
// modes directory is preserved from the original scaffold so future
// per-mode helpers have a home if needed, but no code lives here today.
package modes
